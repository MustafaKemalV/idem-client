/**
 * WebClient integration: the exchange filter that stamps the header, and the wrapper that makes
 * forgetting the filter impossible.
 *
 * <p>Null-marked: unless a type use is explicitly annotated {@code @Nullable}, it is not null, on
 * arguments and on return values alike.
 */
@NullMarked
package io.github.mustafakemalv.idemclient.web;

import org.jspecify.annotations.NullMarked;
